NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
NEXT_PUBLIC_POST_LOGIN_PATH=/dashboard

# Keep false for normal development: Dashboard calls the real backend.
# Enable only for isolated UI demos; mutations are intentionally not persisted.
NEXT_PUBLIC_DASHBOARD_MOCK_MODE=false
DEV_LOGIN_EMAIL=dev@auralis.local
DEV_LOGIN_PASSWORD=change-this-local-password
