import { SettingsModule } from "@/components/settings/SettingsModule";

export const metadata = {
  title: "Cài đặt — Auralis AI",
};

export default function SettingsPage() {
  return <SettingsModule />;
}
